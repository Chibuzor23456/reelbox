import CategoryChannelsPage from '../components/CategoryChannelsPage'

export default function Sports() {
  return (
    <CategoryChannelsPage
      title="Sports"
      category="Sports"
      description="Live sports channels. Availability reflects what's actually live and permitted to stream — not every event airs on every channel."
    />
  )
}
